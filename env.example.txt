# ─────────────────────────────────────────────
# Growing Minds — Environment Variables
# ─────────────────────────────────────────────
# Copy this file to .env.local for local development
# Add all these variables in Vercel Dashboard → Settings → Environment Variables

# ── ANTHROPIC (story text generation) ──
ANTHROPIC_API_KEY=sk-ant-...       # console.anthropic.com

# ── IMAGE GENERATION ──
OPENAI_API_KEY=sk-...              # platform.openai.com — used for illustrations (gpt-image-1)
GEMINI_API_KEY=...                 # aistudio.google.com — used by api/test-character-image.js (prototype)
SKIP_ILLUSTRATIONS=false           # set to "true" to skip DALL-E/gpt-image-1 calls during testing

# ── STRIPE ──
STRIPE_SECRET_KEY=sk_live_...      # Stripe secret key (use sk_test_... for testing)
STRIPE_WEBHOOK_SECRET=whsec_...    # From Stripe Dashboard → Webhooks → your endpoint

# ── APP URLs ──
NEXT_PUBLIC_BASE_URL=https://growingminds.io   # Your live domain (use http://localhost:3000 locally)

# ── LULU DIRECT (print fulfillment) ──
LULU_CLIENT_ID=...                 # From Lulu Developer Portal
LULU_CLIENT_SECRET=...             # From Lulu Developer Portal
LULU_CONTACT_EMAIL=hello@growingminds.io
LULU_SANDBOX=false                 # set to "true" to use Lulu's sandbox (no real prints/charges)

# ── PDF GENERATION ──
PDFSHIFT_API_KEY=...                # pdfshift.io — converts story HTML to the delivered PDF

# ── EMAIL ──
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=Growing Minds <stories@growingminds.io>   # optional, has a code default

# ── STORAGE ──
BLOB_READ_WRITE_TOKEN=...          # Vercel Blob storage token (illustrations, generated PDFs)

# ── REDIS (Upstash — story tokens, chapter drafts, illustration URLs) ──
UPSTASH_REDIS_REST_URL=https://...upstash.io
UPSTASH_REDIS_REST_TOKEN=...

# ── INNGEST (background job orchestration) ──
INNGEST_EVENT_KEY=...              # from the Inngest dashboard, must match the synced app's environment

# ── AIRTABLE (lead capture + story archive, optional) ──
AIRTABLE_BASE_ID=app...
AIRTABLE_TOKEN=pat...               # Personal Access Token with data.records:write scope on this base

# ── ADMIN / DIAGNOSTIC ENDPOINTS ──
ADMIN_WEBHOOK_SECRET=...            # gates api/lulu-jobs.js and api/test-character-image.js
